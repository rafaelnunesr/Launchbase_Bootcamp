{% extends '../layout.njk' %}

{% block header %}
    <title>Foodfy | {{chef.name}}</title>
{% endblock %}

{% block content %}

    <main>
        <div class="admin-chef">
            <div class="chef_photo">
                <img src="{{chef.photo}}" alt="Foto de {{chef.name}}">
            </div>
        </div>

    </main>

{% endblock %}