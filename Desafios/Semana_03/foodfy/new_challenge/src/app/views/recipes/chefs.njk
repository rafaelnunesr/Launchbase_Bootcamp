{% extends './layout.njk' %}

{% block header %}
    <title>Foodfy | Chefs</title>
{% endblock %}

{% block search %}
{% endblock %}

{% block content %}

    <main>

        <div class="chefs">

            {% for chef in chefs %}
                <div class="chef">
                    <div class="chef-photo">
                        <img src={{chef.photo}} alt="Foto de {{chef.name}}">
                    </div>

                    <div class="chef-info">
                        <div class="chef-name">
                            <h2>{{chef.name}}</h2>
                        </div>

                        <div class="chef-qtt-recipes">
                            <p>{{chef.total_recipes}} receitas</p>
                        </div>
                    </div>    
                </div>
            {% endfor %}

        </div>

        <div class="pagination public"
            data-page={{pagination.page}}
            data-total={{pagination.total}}
            data-filter={{filter}}
        ></div>

    </main>


{% endblock %}