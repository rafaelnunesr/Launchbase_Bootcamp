{% extends '../layout.njk' %}

{% block head %}
    <title>Foodfy | {{user.name}}</title>
{% endblock %}

{% block content %}
    <div class="container">
        <form class="user" action="/admin/profile?_method=PUT" method='POST'>
            <div class="sub-header">
                <h1>Editar {{user.name}}</h1>
                <nav>
                     <button 
                        type="submit"
                        class="button">Salvar</button>
                </nav>
            </div>

        {% include './parts/user-fields.njk' %}
    </form>
    </div>
{% endblock %}
