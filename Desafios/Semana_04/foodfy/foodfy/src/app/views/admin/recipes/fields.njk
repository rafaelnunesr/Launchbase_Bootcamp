<div class="admin-recipes-photos-preview">
    <div class="photo-box">
        <input 
            id="photos-recipe-upload"
            type="file"
            name="photos"
            multiple
            accept="image/*"
            onchange="PhotosUpload.addPhotoRecipe()"
            >
            <i 
                class="material-icons"
                onclick="PhotosUpload.addPhotoRecipe()">add</i>
    </div>

    {% if files %}
        {% for image in files %}
            <div class="photo-box">
                <a href="">
                <img src="../../assets/burger.png" alt="burger photo">
                </a>
            </div>
        {% endfor %}
    {% endif %}

   
</div>

<div class="fields-input">
    <div class="input-item">
        <h1>Chef</h1>
        <input 
            type="text">
    </div>

     <div class="input-item">
        <h1>Ingredientes</h1>
        <div class="ingredients-fields">
            <input 
                type="text"
                name="ingredient"
                placeholder="Adicione um novo ingrediente">
        </div>
        <a 
            class="add-ingredient"
            onclick="AddComponentRecipe.addIngredient()">Adicionar ingrediente</a>
    </div>

    <div class="input-item">
        <h1>Preparo</h1>
        <div class="preparation-fields">
            <input 
                type="text"
                name="preparation"
                placeholder="Adicione um novo passo na preparação">
        </div>
        <a 
            class="add-preparation"
            onclick="AddComponentRecipe.addPreparation()">Adicionar preparo</a>
    </div>

    <div class="input-item">
        <h1>Informações adicionais</h1>
        <textarea 
            name=""></textarea>
    </div>

</div>
